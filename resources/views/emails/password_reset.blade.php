<h3>
    <a href="{{ config('app.url') }}">{{ config('app.name') }}</a>
</h3>

いつもSoundAliveをご利用頂き、ありがとうございます。<br>
パスワードをリセットします。<br>
下記のリンクから再設定を行ってください<br>
<br>

<a href="{{ $actionUrl }}">{{ $actionUrl }}</a>
